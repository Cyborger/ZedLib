from setuptools import setup

setup(name='ZedLib',
      version='0.2',
      description='A library built on top of pygame',
      author='Joe Zlonicky',
      packages=['ZedLib'],
      zip_safe=False)
